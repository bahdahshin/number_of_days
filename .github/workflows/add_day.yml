name: Add one day per day
on:
  schedule:
    - cron: "0 0 * * *"
  push:
    
jobs:
  add_one:
    runs-on: ubuntu-latest
    
    steps:
    - run: echo -n -e "HELLO\n !\n World!"
    - run: cat README.md
      
